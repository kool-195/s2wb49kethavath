extends layout

block content

  h1= title
  - var image_names = ["watch1.jpg","watch2.jpg","watch3.jpg","watch4.jpg","watch5.jpg"]
  - for(let i=0; i<3;i++){
    -var num = image_names[Math.floor(Math.random() * 5)]
    
    img(src= "/images/"+num, alt= num, style={width: "100",height: "100"})
  -}