extends layout

block content
  h1= title
  p This is cool #{title}
br
  - var btnType = 'primary'
  - var btnSize = 'lg'
  button(type='button' class='btn btn-' + btnType + ' btn-' + btnSize) Press Me